# Enemy sharing details

## one enemy

enemyId as Extra
```intent.putExtra("enemyId", bitmapComponent._id.toString())```

FIGHT - END - RETURN KILLED ID

## multiple enemy

enemyMulID
```intent.putExtra("enemyMulId", bitmapComponent._id.toString()+....)```

CHOOSE ENEMY - FIGHT - END - RETURN KILLED ID